# string from decision tree (storage O(N), test runtime: O(log N), training runtime: O(DN^2logN))
# no longer look for globally split, K instead of D
# ensemble combination (bayesian model combination)
